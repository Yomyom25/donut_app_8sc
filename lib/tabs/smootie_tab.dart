import 'package:flutter/material.dart';

class SmootieTab extends StatelessWidget {
  const SmootieTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Smootie Tab"),
    );
  }
}